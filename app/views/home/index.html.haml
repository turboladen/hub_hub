.hero-unit
  %h1 MindHub
  %p Connecting California's Central Valley
.row
  .span8
    %table.table.table-condensed
      %caption
        %h3 Recent Posts
      %tbody
        = render partial: 'spokes/post', collection: @posts
  .span3
    .well.sidebar-nav{ style: "height: 100%;" }
      %ul.nav.nav-list
        %li.nav-header Spokes
        - @spokes.each do |spoke|
          %li
            = link_to spoke.name, spoke
