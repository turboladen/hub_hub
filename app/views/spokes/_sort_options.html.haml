- @sort_options.each do |option|
  - if option == @sorter
    %li.active
      = link_to option.to_s.humanize, spoke_path(sort: option)
  - else
    %li
      = link_to option.to_s.humanize, spoke_path(sort: option)

